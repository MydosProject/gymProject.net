@model MembershipPackageFormViewModel
@{
    ViewData["Title"] = "Paket Düzenle";
}

<section class="py-5">
    <div class="container">
        <div class="mb-4">
            <a asp-action="Index" class="text-muted text-decoration-none">← Paketlere dön</a>
            <h1 class="h3 mt-2">Paket Düzenle</h1>
        </div>

        <partial name="_PackageForm" model="Model" />
    </div>
</section>

@section Scripts {
    <partial name="_ValidationScriptsPartial" />
}
