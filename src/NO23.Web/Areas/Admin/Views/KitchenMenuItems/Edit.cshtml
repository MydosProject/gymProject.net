@model KitchenMenuItemFormViewModel
@{
    ViewData["Title"] = "Kitchen Ürünü Düzenle";
}

<section class="admin-page">
    <div class="container">
        <header class="admin-page-head admin-form-head">
            <div>
                <a asp-action="Index" class="admin-back-link">← Menüye dön</a>
                <p class="admin-eyebrow">Operasyon / Kitchen</p>
                <h1>Kitchen ürününü düzenle</h1>
                <p class="admin-page-description">Ürün içeriğini, besin değerlerini, fiyatını ve durumunu güncelle.</p>
            </div>
        </header>

        <partial name="_KitchenMenuItemForm" model="Model" />
    </div>
</section>

@section Scripts {
    <partial name="_ValidationScriptsPartial" />
}
